import styled from "styled-components";
import { colorTexto } from "../Ui/Variables";

const Text=styled.p`
    color: ${colorTexto};
    margin-left: 15px;
    font-size: 18px;

`;

export default Text;